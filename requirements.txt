# Core — zero dependencies for the scanner itself
# Install extras as needed:
#
#   pip install -e .          # core scanner + CLI
#   pip install -e ".[api]"   # + REST API server
#   pip install -e ".[dev]"   # + all dev tools

# ---- REST API (pip install -e ".[api]") ----
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0

# ---- Integrations (optional) ----
# openai>=1.0          # SafeOpenAI guard
# langchain>=0.2       # PromptSentinelGuard
# starlette>=0.37      # FastAPI middleware (included with fastapi)

# ---- Development (pip install -e ".[dev]") ----
pytest>=7.0
pytest-cov>=4.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.4.0
mypy>=1.9
pre-commit>=3.7
